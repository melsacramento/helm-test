apiVersion: v1
kind: Service
metadata:
  name: {{ include "alpine2.fullname" . }}
  labels:
    {{- include "alpine2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alpine2.selectorLabels" . | nindent 4 }}
