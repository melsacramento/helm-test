apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alpine2.fullname" . }}-test-connection"
  labels:
    {{- include "alpine2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alpine2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
